# Как устроен SaqtaDauys

[← На главную](../README.md) · [Сценарий демонстрации](DEMO.md)

У проекта четыре точки входа: браузер, Telegram, текстовый чат и пакетная
маршрутизация. Три интерактивных интерфейса используют один диалоговый движок;
голосовые каналы добавляют распознавание и озвучивание к тому же разговору.

## Карта кода

| Модуль | Ответственность |
|---|---|
| [`src/config.py`](../src/config.py) | Пути, модели, таймауты и лимиты; секреты остаются в окружении. |
| [`src/main_router.py`](../src/main_router.py) | Компактный промпт, вызов классификатора, проверка ID и сохранение предсказаний. |
| [`src/agent/state.py`](../src/agent/state.py) | Активная задача, очередь просьб, отложенные задачи и ожидаемое подтверждение. |
| [`src/agent/catalog.py`](../src/agent/catalog.py) | Чтение данных, проверка полей и маскирование типовых идентификаторов. |
| [`src/agent/numbers.py`](../src/agent/numbers.py) | Локальный разбор цифр и числительных RU/KK для телефонов и ИИН. |
| [`src/agent/llm.py`](../src/agent/llm.py) | Понимание текущей реплики, маршрутизация и формулирование ответа по фактам. |
| [`src/agent/dialog_manager.py`](../src/agent/dialog_manager.py) | Следующий вопрос, смена темы, проверка полиса и выполнение разрешённых действий. |
| [`src/tools/backend.py`](../src/tools/backend.py) | Учебные расчёты, поиск полисов, запись к врачу, заявления и обращения. |
| [`src/tools/audio.py`](../src/tools/audio.py) | Распознавание аудио и синтез голосового ответа. |
| [`src/tools/telegram_api.py`](../src/tools/telegram_api.py) | HTTP-запросы Telegram, загрузка голосовых и отправка ответов. |
| [`src/tools/terminal_qr.py`](../src/tools/terminal_qr.py) | Ссылка и QR из username текущего бота; запасной вывод ссылки без QR. |
| [`src/telegram_bot.py`](../src/telegram_bot.py) | Команды, long polling и отдельный сеанс для каждого чата. |
| [`src/web_demo.py`](../src/web_demo.py) | Локальный HTTP-сервер, изолированные сеансы и поток аудио. |
| [`src/web/`](../src/web/) | Микрофон, текстовый ввод, плеер, панель супервизора и клиентские замеры. |
| [`src/benchmark.py`](../src/benchmark.py) | Парные замеры последовательного и параллельного диалога через API. |
| [`src/chat_demo.py`](../src/chat_demo.py) | Терминальный интерфейс и прогон списков реплик из `examples/`. |

## Путь одной реплики

1. Браузер или Telegram передаёт текст или завершённую голосовую запись.
   Веб принимает WebM/OGG/MP4/WAV, Telegram — OGG/Opus; проверяются размер и формат.
2. Голос преобразуется в текст. При ожидании телефона или ИИН используется
   `gpt-4o-transcribe` с подсказкой о диктовке цифр; обычно — `gpt-4o-mini-transcribe`.
3. Движок локально обрабатывает приветствие, команды управления и однозначный
   ввод ожидаемого идентификатора.
4. LLM определяет язык, тип реплики и поля. Для каждого извлечённого значения
   требуется цитата из текущего сообщения; Python проверяет её и формат поля.
5. Маршрутизатор выбирает сценарий. На новом запросе этот шаг и извлечение полей
   выполняются параллельно: они читают вход, не изменяя состояние или backend.
   Движок сохраняет другие просьбы в очереди,
   учитывает срочность и при необходимости откладывает текущую задачу.
6. Обработчик уточняет недостающие данные, находит клиента и полис, получает факты
   или подготавливает действие для подтверждения.
7. Ответ отправляется текстом и, если включено, голосом. Веб отдаёт MP3 потоком;
   Telegram синтезирует запись одновременно с отправкой текста. Трассировка
   показывает выбранные сценарии, действия и время каждого этапа.
8. В вебе отдельный фоновый запрос объясняет уже выбранные сценарии и называет
   альтернативы. Ответ валидируется: ID и порядок выбранных сценариев должны
   совпасть. Ошибка объяснения не меняет состояние, ответ или маршрутизацию.

В финальной проверке усилены проверки понимания реплики: просьба об операторе
не отменяет активную задачу без явного отказа; известная специальность врача
из соседнего поля услуги переносится в `doctor_specialty` только для записи.
Язык прежнего ответа не задаёт язык новой содержательной реплики. Если большинство
слов содержит характерные казахские буквы, ошибочная метка `ru` исправляется на `kk`;
остальные случаи и смешанная речь остаются на решении LLM.

Объясняющая модель использует отдельную строгую JSON-схему с обязательными
причинами и оценками. Допустимые выбранные ID фиксируются уже принятым решением,
а альтернативы ограничены остальными ID каталога. Дополнительная проверка в Python
сохраняет порядок и диапазон оценок. Основной маршрутизатор при этом не менялся.

Маршрутизатор использует **`gpt-4o-mini`**, температуру **0.1** и JSON mode.
В `predict_intent()` проверяются структура ответа и допустимые ID; при ошибке
возвращается `SYS_UNCLEAR`. Эталонные ответы `expected` и ID тестовых реплик
модели не передаются. Понимание диалоговой реплики использует отдельную строгую JSON-схему.

## Состояние и подтверждения

У каждой задачи собственные поля. Короткая история помогает понять контекст,
но новые значения принимаются с подтверждением из текущей реплики.
Телефоны и ИИН, уже записанные цифрами, разбираются локально с сохранением нулей.
Неполный номер не дополняется предположениями.

В вопросе о пострадавших формулируется одно условие: «Есть ли пострадавшие?».
Короткие «да/нет/есть/бар/жоқ» заполняют только ожидаемое булево поле.
При сообщении о погибшем фильтр цитат сохраняет `injured=true`; отсутствие
погибших само по себе не означает отсутствия пострадавших. Если при ожидании
места клиент пишет компактный адрес вида «Б17 д. 42», он сохраняется дословно
без LLM и проверки по карте. Полный текст новой просьбы под этот разбор не попадает.
При `injured=true` предупреждение об экстренном обращении выдаётся до уточнения
адреса, в том числе если первоначальный условный совет уже прозвучал.

Перед действием, требующим согласия, движок сохраняет конкретную операцию и её
параметры в `PendingAction`. Явное подтверждение выполняет именно эту операцию.
Исправление данных или ошибка отменяет прежнее подтверждение и требует нового
согласования. Повторное «да» после завершения не должно создавать вторую запись.
Сбой загрузки голосового сообщения и слишком длинная реплика тоже снимают
подтверждение: движок не должен выполнять прежнюю операцию после непрочитанного исправления.

Передача оператору имеет отдельное состояние. Ответ «да, подключайте» не запускает
снова отложенную задачу. Возврат к ней требует явной просьбы. Передача учебная:
живого оператора за ботом нет, и бот сообщает об этом пользователю.

## Данные и исполнение

Учебная дата **2026-10-01** задаётся датасетом. Каждый сеанс получает отдельную
копию mock backend; исходные JSON-файлы остаются неизменными. `/reset`, перезапуск
процесса или час бездействия сбрасывают данные разговора.

Для 33 бизнес-сценариев есть обработчики. У `SC02`, `SC04`, `SC05`, `SC06`, `SC12`,
`SC27`, `SC28` самостоятельного исполнения нет: используется учебная передача
оператору. Наличие обработчика не означает реализацию всей цепочки действий из ТЗ.
Например, консультация и расчёт КАСКО не оформляют настоящий полис.
Поля для расчёта запрашиваются с учётом контракта действия: франшиза нужна
для расчёта КАСКО, хотя в сценарии консультации она отмечена как необязательная.
Для вопроса об оплате конкретного продукта модель получает его условия рассрочки.

В исходных данных нет расписания клиник, поэтому для демо используются часы
10:00, 11:00, 14:00 и 16:00 по рабочим дням. Идентификация по телефону упрощена
для кейса; реальные SMS, email и переводы звонков не отправляются.

## Браузер и трассировка

Сервер стандартной библиотеки `ThreadingHTTPServer` слушает `127.0.0.1`.
Статика и API имеют один origin; проверяются Host/Origin, применяется CSP.
Случайный HttpOnly/SameSite cookie связывает запрос с сеансом. Один сеанс
обрабатывает одну реплику одновременно; разные сеансы имеют отдельные движки
и копии backend. Максимум 16 сеансов, четыре основных операции и два фоновых
объяснения одновременно. При перегрузке запрос получает явный отказ, а не бесконечную очередь.

`POST /api/turn` принимает текст или запись и возвращает текст, трассировку
и адрес аудио. `GET /api/audio/<id>` запускает потоковый TTS; `GET /api/trace/<id>`
обновляет метрики и объяснение. Доступ к реплике требует cookie её сеанса.
`POST /api/reset` сбрасывает историю и учебные действия. Записи последних десяти
реплик хранятся в памяти; повторный запрос готового аудио не вызывает TTS снова.

Объяснение строится после выбора отдельным LLM-запросом. Оно может оспорить
соответствие фразы сценарию, но не переписать выбранные ID. Оценка confidence
относится к объяснению, а не к калиброванной вероятности маршрутизатора.
Локальные шаги диалога имеют объяснение из кода, без лишнего LLM-вызова.

`playing` измеряется на клиенте, время последней голосовой активности оценивается
по энергии микрофона. Серверные стадии, конец синтеза и начало воспроизведения
разделены. [Методика и измерения](LATENCY.md).

## Telegram и хранение

- Обрабатываются личные чаты, до 64 активных сеансов в одном процессе.
- Сообщения проходят последовательно. Голосовая запись ограничена 60 секундами
  и 10 МБ, текст — 4000 символами.
- Аудио обрабатывается в памяти. Telegram сохраняет в `.runtime/` номер обновления,
  без текста, аудио и ключей. Отдельно диагностические команды могут сохранять
  туда отчёты замеров.
- Этот номер записывается **до обработки**: после перезапуска действие не должно
  повториться. Если процесс остановился посередине ответа, реплику нужно отправить заново.
- `total_processing` включает обработку и отправку ответов. Это не измерение
  времени до первого звука на устройстве пользователя.

Реплики, короткая история и голос отправляются в OpenAI для обработки; сообщения
остаются в самом Telegram-чате. Типовые телефоны, ИИН и email маскируются
в ответах и трассировках, но это не универсальный фильтр персональных данных.
Для публичного показа используются вымышленные клиенты из `/demo`.

## Работа с кодом

Настройки стиля находятся в [`pyproject.toml`](../pyproject.toml): Ruff,
единое оформление импортов и строк, целевая версия Python 3.10.

```bash
python -m pip install -r requirements-dev.txt
ruff check src tests
ruff format --check src tests
python -m unittest discover -s tests -v
```

Применить форматирование: `ruff format src tests`. Для автоматического исправления
простых замечаний: `ruff check --fix src tests`.

Локальные тесты подменяют API и проверяют контракты, состояние и действия.
Они не измеряют качество живого LLM-диалога. В свободных прогонах замечены ошибки
выбора суммы из нескольких чисел, языка ответа и продолжения разговора после расчёта;
этим случаям нужны отдельные проверки и доработка.

Материалы кейса: [описание данных](../data/README.md),
[сценарии](../data/scenarios.json), [действия](../data/actions.json),
[база знаний](../data/knowledge_base.json).
